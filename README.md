# Agedap-Llamatron

DEMO ONLY

npm install
npm run electron (probar)
npm run make (crear ejecutable)

Pasos:

1. En 'Test Page' clic en load llama y esperar hasta que cargue llama
2. Introducir ruta absoluta al modelo y clic en load model, esperar hasta que cargue
3. Ir a la página 'Chat' y esperar a que cargue
4. Conversar
